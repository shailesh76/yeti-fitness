import Constants from 'expo-constants';

export const APP_VERSION = Constants.expoConfig?.version || '1.0.0';
export const BUILD_IDENTIFIER = '2026.08.23-beta5';
export const APP_BUILD_LABEL = `v${APP_VERSION} (${BUILD_IDENTIFIER})`;
